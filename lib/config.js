// Общие параметры пайплайна.

// Максимальный возраст новости (по дате публикации у источника). Всё старше —
// не парсим и не публикуем. Защита от протухших/архивных лент.
// 10 дней: убивает архив/«2022», но терпит редкий темп weird-разделов
// (NY Post, Metro публикуют пару раз в неделю).
export const MAX_AGE_DAYS = 10;

// Минимальный интервал между публикациями в канал.
export const MIN_GAP_MINUTES = 90;
